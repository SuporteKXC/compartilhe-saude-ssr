import { NoSubmitWithEnterDirective } from './no-submit-with-enter.directive';

describe('NoSubmitWithEnterDirective', () => {
  it('should create an instance', () => {
    const directive = new NoSubmitWithEnterDirective();
    expect(directive).toBeTruthy();
  });
});
